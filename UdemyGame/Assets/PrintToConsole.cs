using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PrintToConsole : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("콘솔에 출력중입니다");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
